// DISABLED - This component was causing overlay conflicts
// Original loading screen component moved to avoid z-index issues
export default function LoadingScreen() {
  return null; // Disabled to prevent conflicts
}